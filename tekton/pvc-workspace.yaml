apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jobfit-pipeline-workspace
  namespace: jobfit
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn-fast
  resources:
    requests:
      storage: 2Gi
