# Placeholder Secret, unused today. No API keys are needed while the agent
# runs against local Ollama. Wired into the Deployment/CronJob env now so
# that adding a hosted-LLM API key later (e.g. HOSTED_LLM_API_KEY) needs
# only `kubectl create secret` / editing this file, not a manifest change
# to deployment-web.yaml or cronjob-worker.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: jobfit-secrets
  namespace: jobfit
type: Opaque
data: {}
